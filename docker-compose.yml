version: '3.8'

x-backend-base: &backend-base
  build:
    context: .
    dockerfile: Dockerfile.backend
  # Override with compose-specific overrides over the local .env
  environment:
    - SENTINEL_KAFKA_HOST=kafka:9092
    - SENTINEL_DB_HOST=postgres
    - SENTINEL_DB_PORT=5432
    - SENTINEL_DB_USER=sentinel
    - SENTINEL_DB_PASS=sentinel_pass
    - SENTINEL_DB_NAME=sentinel_db
    - SENTINEL_DB_POOL_MAX=20
    - SENTINEL_INSERT_BATCH_SIZE=1000
  depends_on:
    postgres:
      condition: service_healthy
    kafka:
      condition: service_healthy

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: sentinel
      POSTGRES_PASSWORD: sentinel_pass
      POSTGRES_DB: sentinel_db
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sentinel -d sentinel_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - pgdata:/var/lib/postgresql/data

  kafka:
    image: bitnami/kafka:3.5
    ports:
      - "9092:9092"
    environment:
      - KAFKA_CFG_NODE_ID=1
      - KAFKA_CFG_PROCESS_ROLES=controller,broker
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=1@kafka:9093
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - ALLOW_PLAINTEXT_LISTENER=yes
    healthcheck:
      test: ["CMD", "kafka-topics.sh", "--bootstrap-server", "127.0.0.1:9092", "--list"]
      interval: 10s
      timeout: 5s
      retries: 10

  api_server:
    <<: *backend-base
    ports:
      - "8000:8000"
    command: ["uvicorn", "api_server:app", "--app-dir", "src", "--host", "0.0.0.0", "--port", "8000"]

  kafka_consumer:
    <<: *backend-base
    working_dir: /app/src
    command: ["python", "-m", "kafka_to_postgres"]
    # Waits for DB to be healthy so migration won't crash

  feature_builder:
    <<: *backend-base
    working_dir: /app/src
    command: ["python", "-m", "feature_builder"]
    depends_on:
      kafka_consumer:
        condition: service_started
      postgres:
        condition: service_healthy

volumes:
  pgdata:
